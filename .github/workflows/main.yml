name: Deploy to Dev Databricks Workspace

on:
  push:
    paths-ignore:
      - .github/**
    branches:
      - main

jobs:
  dev-deploy:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Extract Commit Author Email and set REPOSITORY_PATH
        id: extract_email
        run: |
          AUTHOR_EMAIL=$(git log -1 --pretty=format:'%ae')
          echo "AUTHOR_EMAIL=$AUTHOR_EMAIL" >> $GITHUB_ENV
          echo "REPOSITORY_PATH=/Workspace/Users/$AUTHOR_EMAIL/databricks-repo" >> $GITHUB_ENV

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Setup Databricks CLI
        uses: databricks/setup-cli@main

      - name: Extract Branch Name
        id: extract_branch
        run: echo "BRANCH_NAME=${GITHUB_REF#refs/heads/}" >> $GITHUB_ENV

      - name: Update Databricks Repo (Dev workspace)
        run: |
          databricks repos update "${{ env.REPOSITORY_PATH }}" --branch "${BRANCH_NAME}"